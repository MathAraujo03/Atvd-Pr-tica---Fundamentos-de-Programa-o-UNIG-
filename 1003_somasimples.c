#include <stdio.h>

int main() {
    int A, B, SOMA;

    // Leitura dos valores inteiros
    scanf("%d %d", &A, &B);

    // Cálculo da soma
    SOMA = A + B;

    // Exibição do resultado
    printf("SOMA = %d\n", SOMA);

    return 0;
}